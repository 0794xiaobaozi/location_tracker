[workspace]
authors = ["0794xiaobaozi <tonic.song@icloud.com>"]
channels = ["conda-forge"]
name = "LocationTracking"
platforms = ["win-64"]
version = "0.1.0"

[environments]
location-tracker = { features = ["location-tracker"] }

[tasks]
select-intervals = "python SelectVideoIntervals.py"

[dependencies]

[feature.location-tracker.dependencies]
python = "3.12.*"
pip = "*"
numpy = "*"
pandas = "*"
scipy = "*"
matplotlib = "*"
tqdm = "*"
opencv = "*"
pillow = "*"
holoviews = ">=1.22.1,<2"
bokeh = ">=3.9.0,<4"
pyyaml = "*"

[feature.location-tracker.pypi-dependencies]
ipykernel = "*"
customtkinter = "*"

# GUI + OpenCV live in this env; default workspace has no Python on PATH, so run with: pixi run -e location-tracker merge-dual-camera-gui
[feature.location-tracker.tasks]
merge-dual-camera-gui = "python MergeDualCameraVideosGUI.py"
merge-dual-camera = "python MergeDualCameraVideos.py"
